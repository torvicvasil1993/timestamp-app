apiVersion: template.openshift.io/v1
kind: Template
metadata:
  name: pvc-template
objects:
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: timestamp-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
  storageClassName: gp2
  volumeMode: Filesystem
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: timestamp-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: timestamp-app
  template:
    metadata:
      labels:
        app: timestamp-app
    spec:
      containers:
        - name: timestamp-app
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: timestamp-app
              mountPath: /app/
      volumes:
        - name: timestamp-app
          persistentVolumeClaim:
            claimName: timestamp-app
---
apiVersion: v1
kind: Service
metadata:
  name: timestamp-app
spec:
  selector:
    app: timestamp-app
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: timestamp-app-route
spec:
  to:
    kind: Service
    name: timestamp-app
  port:
    targetPort: http
  path: /

